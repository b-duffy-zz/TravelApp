import {processCity} from '../src/client/js/formHandler'
import {submit} from '../src/client/js/formHandler'

describe("Testing the UI update functionality", () => {
    test("Testing the processLanguage() function", () => {
        expect(processCity).toBeDefined();
    }
    )})

