import {resetUI} from '../src/client/js/resetUI'

describe("Testing the reset UI  functionality", () => {
    test("Testing the resetUI() function", () => {
        expect(resetUI).toBeDefined();
    }
    )})